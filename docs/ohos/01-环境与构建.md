# 鸿蒙（OpenHarmony）适配笔记

> 适用：需要构建/调试鸿蒙 HAP、或排查鸿蒙侧依赖问题时。
> **本篇与 docs/01~16 相互独立**：那 16 篇描述的是 Android / Windows 主线的架构与规范，
> 本篇只记录鸿蒙这条支线的环境、依赖、构建方式与踩坑。
>
> 当前定位：**鸿蒙是补充平台，不是重点**。Android / Windows 仍是主线，
> 任何鸿蒙改动都不得影响主线构建（见「与主线的隔离」）。
>
> **换机器请注意**：文中出现的 `C:\Users\admin\...`、`C:\Program Files\Huawei\...` 等是本机实测路径，
> 换成你自己的路径即可。**从零部署**（别人克隆本仓库后怎么配环境、怎么打包）见
> [02-从零部署指南.md](./02-从零部署指南.md)。

## 0. 现状快照（2026-09）

| 项 | 状态 |
|---|---|
| 可行性 | ✅ 已验证：能出包、能安装、能启动到首页，只读链路（列表/帖子详情/图片缓存/设置）正常 |
| 登录 | ⚠️ 已接入适配插件，**待实测**（涉及 cookie 是否正确写回 Dio） |
| 编辑器选图/上传 | ⚠️ 已接入适配插件，**待实测** |
| 真机 arm64 | ⚠️ 未验证（目前只在 x64 模拟器上跑过） |
| 产物 | `build/ohos/hap/entry-default-{signed,unsigned}.hap` |

## 1. 环境要求

### 1.1 DevEco Studio 侧

| 组件 | 版本（实测） | 说明 |
|---|---|---|
| DevEco Studio | 26.0（`DS-261.23567.138.36.2600821`） | 安装后需登录华为账号以**自动签名**（见 1.4） |
| HarmonyOS SDK | API 26 / platformVersion 26.0.0 | DevEco 内置 `sdk\default\openharmony` |
| ohpm | 26.0.0.630 | DevEco 内置 |
| hvigor | 6.26.4 | DevEco 内置 |
| node | v24.14.1 | DevEco 内置，勿用系统 node |
| hdc | 3.2.0f | 位于 OpenHarmony SDK 的 `toolchains` |
| JDK | 17 | |

### 1.2 Flutter-OH SDK

鸿蒙用的是 Flutter 的**分支 SDK**（不是插件），与官方 SDK 并存互不冲突：

```powershell
# 注册 fork 别名（只需一次）
fvm fork add flutter_ohos https://gitcode.com/CPF-Flutter/flutter_flutter.git

# 安装（首次约 2.8 GB + 引擎产物）
fvm install flutter_ohos/oh-3.44.9-dev
```

选用 `oh-3.44.9-dev`（= `3.44.9+ohos-0.0.1-canary1` / Dart **3.12.2**）的原因：
`pubspec.yaml` 的 `sdk: ^3.12.2` 要求 Dart ≥ 3.12.2，而稳定的 `oh-3.35.7`（Dart 3.9.2）**不满足**。

> 2026-09 实测该仓库只有 `oh-3.41.9-dev` / `oh-3.41.9-release` / `oh-3.44.9-dev` 三条分支。

### 1.3 环境变量（每个新终端都要设，未写系统变量）

```powershell
$d = 'C:\Program Files\Huawei\DevEco Studio'
$env:DEVECO_SDK_HOME = "$d\sdk"
$env:PATH = "$d\tools\ohpm\bin;$d\tools\hvigor\bin;$d\tools\node;" +
            "C:\Users\admin\AppData\Local\OpenHarmony\Sdk\26.0.0\toolchains;$env:PATH"
$env:FLUTTER_OHOS_STORAGE_BASE_URL = 'https://flutter-ohos.obs.cn-south-1.myhuaweicloud.com'
$env:FLUTTER_GIT_URL = 'https://gitcode.com/CPF-Flutter/flutter_flutter.git'  # 可选，消除 doctor 警告
```

### 1.4 签名

- **调试安装**：DevEco Studio 打开 `ohos/` 目录 → File → Project Structure → Signing Configs →
  勾选 *Automatically generate signature*（**需要登录华为开发者账号**）。
  配置写入 `ohos/build-profile.json5` 的 `signingConfigs`，证书落在 `~/.ohos/config/`。
- **侧载分发**：出**无签名**包（`--no-codesign`），由使用者自行签名，见第 3 节。
- 校验签名是否配置正确：`signingConfigs` 非空 + profile 的 `bundle-name` 与
  `ohos/AppScope/app.json5` 的 `bundleName` 一致 + profile 的 `device-ids` 含目标设备 UDID
  （`hdc shell bm get --udid`）。

## 2. 依赖适配

pub.dev 上的插件没有 `ohos` 实现，必须换成 OpenHarmony 社区维护的 fork。
**这些覆盖是全局的**，因此必须用开关方式管理（见第 4 节）。

| 插件 | 上游版本 | OH fork | 分支 | 差异 |
|---|---|---|---|---|
| `path_provider` | 2.1.6 | `openharmony-tpc/flutter_packages` | `br_path_provider-v2.1.5_ohos` | 降 1 个小版本 |
| `url_launcher` | 6.3.2 | `openharmony-tpc/flutter_packages` | `br_url_launcher-v6.3.2_ohos` | 无 |
| `flutter_inappwebview` | 6.1.5 | `openharmony-sig/flutter_inappwebview` | `br_v6.1.5_ohos` | 无（版本一致） |
| `file_picker` | 8.3.7 | `openharmony-sig/fluttertpc_file_picker` | `br_v8.0.7_ohos` | **降 3 个小版本**（OH 侧无 8.3.7 分支） |
| `web`（传递依赖） | 1.1.1 | — | — | 见下方冲突说明 |

**`web` 冲突**：`file_picker 8.0.7` 要求 `web ^0.5.1`，而 `flutter_inappwebview_web` 与上游
`file_picker 8.3.7` 都要求 `web ^1.x`。解法是在覆盖里把 `web` 钉到项目原本已锁定的 `^1.1.0`。
`web` 包只服务 Web 平台，不影响 Android / Windows / 鸿蒙构建。
根治办法是给 fork 提 PR 把 `web: ^0.5.1` 改为 `^1.1.0`（仅一行）。

**无需处理的依赖**：

- `sqflite`（`flutter_cache_manager` 的传递依赖）：无 ohos 实现，但
  `flutter_cache_manager` 在非 Android/iOS/macOS 平台走 `JsonCacheInfoRepository`，
  **鸿蒙运行时不触发 sqflite**。
- `window_manager`：桌面专用，代码里已有 `Platform.isWindows` 守卫，鸿蒙自动空转。

## 3. 构建

### 3.1 手工命令（已验证）

> 尝试过把整套流程封装成一键脚本，但此环境下失败点太多（符号链接、提权、hvigor 缓存目录），
> 脚本可靠性达不到日常使用标准，**已放弃脚本方案**，改为下面的手工步骤。
> 踩过的坑见第 5 节，遇到时按那里的说明处理。

```powershell
# 0) 设环境变量（见 1.3）
# 1) 生成依赖覆盖（模板 → 生效文件）
Copy-Item ohos\pubspec_overrides.ohos.yaml pubspec_overrides.yaml -Force

$flutter = 'C:\Users\admin\fvm\versions\flutter_ohos\oh-3.44.9-dev\bin\flutter.bat'

# 2) 解析依赖（末尾可能报符号链接错误，无害；若报错就再跑一次）
& $flutter pub get

# 3) 出包：模拟器用 x64，真机用 arm64；侧载分发加 --no-codesign
& $flutter build hap --release --target-platform ohos-arm64 --no-codesign
& $flutter build hap --debug   --target-platform ohos-x64

# 4) 用完必须恢复主线（否则 Android / Windows 编不过，原因见第 4 节）
Remove-Item pubspec_overrides.yaml -Force
flutter pub get

# 5) 若上一步崩在符号链接错误，Windows 侧生成文件可能残留鸿蒙痕迹（少了
#    flutter_inappwebview_windows），用 git 还原即可
git checkout -- pubspec.lock windows/flutter/generated_plugin_registrant.cc windows/flutter/generated_plugins.cmake
```

`--target-platform` 可选 `ohos-arm64`（默认）/ `ohos-arm` / `ohos-x64`，**一次只出一个架构**。
步骤 2 与 3 失败时**先原样重跑一次**，多数是环境竞态而非配置错误。

### 3.2 「无签名 + 按架构分离」实测数据

| 配置 | 产物 | 体积 | 包内 libs |
|---|---|---|---|
| `debug --target-platform ohos-x64` | `entry-default-signed.hap` | **152 MB** | 单架构 x64 |
| `release --target-platform ohos-arm64 --no-codesign` | `entry-default-unsigned.hap` | **32.6 MB** | 仅 `libs/arm64-v8a` |
| `release --target-platform ohos-x64 --no-codesign` | `entry-default-unsigned.hap` | **35.2 MB** | 仅 `libs/x86_64` |

要点：

- **体积主要由 `debug`/`release` 决定**（debug 含 VM/JIT 与调试符号），release 比 debug 小约 78%
- **架构必须靠 `--target-platform` 手动分离**，一次构建只产出一个架构的 libs，不存在"一个包含多架构"
- arm64 release 的 libs 构成：`libapp.so` 13.4 MB（AOT Dart）+ `libflutter.so` 16.2 MB（引擎）
  + `libc++_shared.so` 1.2 MB —— 引擎占一半，已无进一步压缩空间
- `--no-codesign` 会提示 `Building for device with codesigning disabled`，属预期；
  产物为 `entry-default-unsigned.hap`，侧载时由使用者自行签名

### 3.4 安装

```powershell
$hdc = 'C:\Users\admin\AppData\Local\OpenHarmony\Sdk\26.0.0\toolchains\hdc.exe'
& $hdc install -r build\ohos\hap\entry-default-signed.hap
```

> 未签名的包 `install` 会失败，需先签名。

## 4. 与主线的隔离（重要）

### 4.1 为什么必须隔离

`dependency_overrides` 是**全局生效**的：只要 `pubspec_overrides.yaml` 存在，
Android / Windows 构建也会用 fork 依赖，而 **`flutter_inappwebview` 的 OH fork 硬编码了
`TargetPlatform.ohos`**（官方 Flutter 框架没有该枚举），会导致官方 SDK 编译直接失败：

```
Error: Member not found: 'ohos'.
  case TargetPlatform.ohos:
```

实测：官方 Flutter 3.44.2 执行 `flutter build apk --debug` → `assembleDebug` FAILED。
此外 `flutter_inappwebview` 的 fork 不含 `flutter_inappwebview_windows`，
Windows 平台插件注册也会告警。

**结论：鸿蒙依赖覆盖必须"用时生成、用完删除"，不能长期留在工作区。**

### 4.2 落地方式

同一份 `lib/` 业务代码，两端共用；两端只是 **SDK 与依赖不同**，靠"覆盖文件用时生成、用完删除"隔离：

| 文件 | 角色 |
|---|---|
| `ohos/pubspec_overrides.ohos.yaml` | **模板**，常驻仓库，本身不生效 |
| `pubspec_overrides.yaml` | 真正生效的文件，**已被 `.gitignore` 忽略**，出鸿蒙包时从模板复制生成 |

流程（命令见 3.1）：

```
出鸿蒙包：Copy-Item 模板 → pub get → build hap → Remove-Item 覆盖 → 官方 pub get 复位
日常     ：什么都不用做，工作区就是干净的主线状态
```

**关键：`pubspec_overrides.yaml` 绝不能常驻**，否则 Android / Windows 直接编不过（4.1）。
恢复是否干净，用两条命令自检：

```powershell
Select-String -Path pubspec.lock -Pattern 'gitcode'   # 应为 0 条
git status --short                                    # 不应出现 pubspec.lock / windows/flutter/generated_* 的改动
```

> 如果更希望两个平台彻底物理隔离（各自独立目录、可同时构建），可另开
> `git worktree`：`git worktree add ../mtbbs-ohos <branch>`。
> 注意 worktree 共享同一个 `.git`，业务代码仍是一份（按提交同步）。

## 5. 已知坑

1. **`file_picker` 插件符号链接创建失败（高频，最烦人）**——`pub get` / `build hap` 可能报：
   `Cannot create link ... \.plugin_symlinks\file_picker` / `PathNotFoundException (errno 2)`，
   整个 flutter 进程直接崩掉。`file_picker` 从 hosted 依赖变成 git 依赖后必现。
   **处理**：
   - 该错误发生在**依赖已解析、`.flutter-plugins-dependencies` 已写入**之后，因此**无害**；
   - **原样重跑一次基本就过**（实测 release 首次失败、重跑成功；Android 侧一次就过）；
   - 不要试图"预建好链接让它跳过"：`New-Item -ItemType SymbolicLink` 在 PS 5.1 里
     **需要管理员权限**（不带免提权标志），未提权一定失败；而 Dart 侧创建又报 errno 2。
     实测这条路走不通，别浪费时间。
   - 想彻底避免：用管理员权限/开启开发者模式的终端执行，或改用 `git worktree` 让鸿蒙独占一个目录。
     实测**开发者模式已开启**（`AllowDevelopmentWithoutDevLicense=1`）也仍然复现，故首选"重试"。
2. **`undefined (reading 'filter')` 报错（DevEco 里点构建必踩）**——症状：

   ```
   > hvigor ERROR: 00305010 Syntax Error
   Error Message: TypeError Cannot read properties of undefined (reading 'filter')
   0 task in total: 0 executed, 0 up-to-date.
   ```

   报错让你去查 `hvigorconfig.ts` / `hvigorfile.ts`，**跟那两个文件毫无关系**。
   真实根因：`ohos/node_modules/flutter-hvigor-plugin` 读 `.flutter-plugins-dependencies`
   的 `plugins.ohos` 后直接 `.filter(...)`，而该文件**没有 `ohos` 段**。

   什么时候会没有：`.flutter-plugins-dependencies` 由 flutter 工具生成，
   **官方 SDK** 生成的清单不含 `ohos`（主线态），**Flutter-OH SDK** 生成的才含。
   所以只要工作区处于主线态（例如刚做完第 4 节的恢复），在 DevEco 里构建就会撞上。

   **解法**：先让工作区进入鸿蒙态，再构建——

   ```powershell
   Copy-Item ohos\pubspec_overrides.ohos.yaml pubspec_overrides.yaml -Force
   & $flutter pub get        # $flutter 为 Flutter-OH SDK 的 flutter
   # 自检：应当输出 file_picker / flutter_inappwebview_ohos / path_provider_ohos / url_launcher_ohos
   (Get-Content .flutter-plugins-dependencies -Raw | ConvertFrom-Json).plugins.ohos.name
   ```

   **建议**：鸿蒙出包走 `flutter build hap`（它自己会保证清单正确），
   DevEco 只用来配签名/安装，避免在两种状态间反复切换。
3. **沙箱/权限**——hvigor 要写 `~/.hvigor/project_caches`，在受限环境会报
   `EPERM: operation not permitted, mkdir 'C:\Users\admin\.hvigor\...'`。
   构建需在普通（非受限）终端执行。
4. **`flutter create` 的副作用**——会生成 `test/widget_test.dart`（模板用例，引用不存在的
   `MyApp`）并改动 `.metadata`，需按需清理。
5. **`file_picker` 版本缺口**——OH 侧只有 8.0.7 / 10.2.0 / 10.3.8 分支。当前选 8.0.7
   （同大版本、API 兼容，`flutter analyze lib` 通过）。若将来要用 10.x，需同步改业务代码。
6. **`flutter_inappwebview` 的 keep-alive 缺陷**——OH fork 未正确实现
   `InAppWebViewKeepAlive`：页面从路由栈弹出后原生 WebView 被销毁，再次进入会重新加载。
   Android/iOS 正常。属社区 fork 缺陷，只能等上游修。
7. **`version.ps1` 可复用**——版本号脚本与 `mtbbs_release.json` 与平台无关，HAP 构建可直接沿用。
8. **release 构建偶发首次失败**——实测首次 `build hap --release` 在 `assembleHap` 阶段退出 1
   且只有 `ProcessException`、无有效错误信息（crash log 也只记录 hvigorw 退出码）；
   **原样重跑即成功**。遇到时先重试一次，不要急着改配置。

## 6. 待办

- [ ] 实测登录：能登录 + 登录后 cookie 正确写回 Dio + 个人页数据正常
- [ ] 实测编辑器选图/上传（`file_picker` + MT 图床）
- [ ] 真机 arm64 安装验证
- [ ] 给 `file_picker` OH fork 提 PR：`web: ^0.5.1` → `^1.1.0`（去掉 `web` 覆盖）
